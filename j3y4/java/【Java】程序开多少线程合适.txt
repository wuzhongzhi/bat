Java程序开多少线程合适？
CPU核心数：开启过多的线程，可能会导致线程间频繁切换，从而影响程序性能。
通常，可以开启的线程数应该小于等于CPU核心数。
任务类型：不同类型的任务需要不同数量的线程。
一般来说，计算密集型的任务（如矩阵计算）需要较少的线程，
而I/O密集型的任务（如网络请求）需要更多的线程。
内存占用：线程的创建和销毁需要消耗一定的内存资源。
系统负载：如果系统负载过高，开启过多的线程会加重系统负担，导致系统性能下降。
微信公众号关注科技青莲获取面试资料
