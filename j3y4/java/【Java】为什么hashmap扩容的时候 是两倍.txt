Java为什么hashmap扩容的时候是两倍？
为了保证散列分布的均匀性，HashMap使用了取模运算（%）将哈希值映射到数组的下标上。
而数组的长度为2的幂次方时，对于任何正数n，n%2^n的结果等于n的二进制表示的低n位，
这是因为2^n的二进制表示是最高位为1，其它位为0，n-1的二进制表示是低n位为1，
其它位为0，所以n%2^n的结果就是n的低n位。
这样，原来的哈希值在新的数组中仍然能够保持相同的低位，高位的变化也不会对低位产生影响，
这就保证了散列分布的均匀性。
微信公众号关注科技青莲获取面试资料
