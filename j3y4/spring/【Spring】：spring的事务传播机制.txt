【Spring】：spring的事务传播机制
当一个方法被定义为事务性方法时，Spring会为该方法创建一个代理对象，
代理对象将被插入到方法调用链中，在方法调用之前开启事务，在方法调用之后提交或回滚事务。
当该方法调用其他事务性方法时，Spring的事务传播机制会根据定义的传播行为来决定事务的行为。
例如，如果调用其他事务性方法时，使用了REQUIRED传播行为，
则当前事务会将新的方法调用纳入到同一个事务中进行提交或回滚。
如果使用了REQUIRES_NEW传播行为，则新的方法调用会开启一个新的事务。
微信公众号关注科技青莲获取面试资料
