【Redis面试题】：redis事务是如何实现的
使用MULTI命令开启事务
依次将多个命令入队列，这些命令并不会立即执行，而是保存在一个队列中
使用EXEC命令执行入队的命令，这些命令会按照入队的顺序依次执行。
在执行之前，Redis会将这些命令打包成一个事务，并在执行期间保持事务的原子性
如果在执行事务期间发生错误，Redis会立即中止事务的执行，并且回滚事务中已经执行的命令。
如果所有命令都执行成功，事务会顺利结束。
微信公众号关注科技青莲获取面试资料
