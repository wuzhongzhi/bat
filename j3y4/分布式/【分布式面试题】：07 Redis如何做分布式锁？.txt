【分布式面试题】：07 Redis如何做分布式锁？
客户端尝试使用set命令来获取锁，如果返回成功，说明获取锁成功；
如果返回失败，说明该锁已经被其他客户端持有，获取锁失败。
在获取锁成功后，客户端可以在Redis中设置一个超时时间，以防止锁被一直持有，
从而导致死锁的问题。超时时间一般设置为锁的有效期，即锁的最长持有时间。
当客户端不再需要锁时，需要使用del命令来释放锁。此时需要判断是否是自己持有的锁，
以防止误释放其他客户端持有的锁。
微信公众号关注科技青莲获取面试资料
