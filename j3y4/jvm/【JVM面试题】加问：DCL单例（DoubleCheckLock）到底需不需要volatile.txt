【JVM面试题】加问：DCL单例（DoubleCheckLock）到底需不需要volatile
在DCL实现中，如果不使用volatile关键字，
则可能会存在指令重排序的问题。在没有volatile修饰的情况下，
可能会先将单例对象的引用赋值给singleton变量，然后再去调用对象的构造方法，
这就会出现问题，因为构造方法中可能会使用到还未初始化的对象。
而volatile关键字可以禁止指令重排序，保证对象的初始化在引用赋值之前完成，从而避免了该问题。
微信公众号关注科技青莲获取面试资料
